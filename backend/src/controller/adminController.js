const { Apartment, ResidentApartment, Resident, User, Fee, Utility_bill, Invoice, InvoicePayment, Vehicles, FeedbackUser} = require('../models/index');
const { Op } = require('sequelize');
const xlsx = require('xlsx');

module.exports = {};
